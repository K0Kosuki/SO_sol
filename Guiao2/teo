FIFO
ser preservar fronteira DAS MSES

*crear uma formula 

int pipe(int pl[2]);
int main(){
int pd[2];
pipe(pd);
pid_t pid;
pid = fork();
if(pid==0){
pd[0]=3;
pd[1]=4;
char buffer(512);
int n =0;
read(jrd[0],burf,sizeof(buffer));
write(1,buffer;n);
close(pd[]);
exit(0);
}
write(pd[1],"ola",3);
close pd[1];
return 0;
}
